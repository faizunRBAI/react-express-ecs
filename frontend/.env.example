# Frontend environment variables (copy to .env.local for local development)
# Vite exposes only VITE_* prefixed vars to the browser

# Backend API URL (empty = same origin in production behind CloudFront)
VITE_API_URL=http://localhost:3000
